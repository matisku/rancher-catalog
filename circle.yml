machine:
  python:
    version: 2.7.12
  environment:
    scratch: /scratch/integration

dependencies:
  pre:
    - sudo mkdir -p ${scratch}
    - python --version
    
test:
  override:
    - sudo -H scripts/ci
