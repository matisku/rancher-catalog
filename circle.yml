machine:
  python:
    version: 2.7.12
  environment:
    scratch: /scratch/integration

dependencies:
  pre:
    - sudo apt-get install python-dev
    - sudo mkdir -p ${scratch}
    - python --version
    
test:
  override:
    - sudo -H scripts/ci
