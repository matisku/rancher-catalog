machine:
  environment:
    scratch: /scratch/integration

dependencies:
  pre:
    - sudo mkdir -p ${scratch}
    - python --version
    
test:
  override:
    - sudo -H scripts/ci
    - cat /scratch/integration/.tox/flake8/log/flake8-1.log
