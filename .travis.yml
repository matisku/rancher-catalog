--- 
sudo: required

language: python
python: 2.7.12

env:
  - scratch="/scratch/integration"

before_install:
  - sudo mkdir -p ${scratch}
  
install:
  - sudo apt-get install python-dev
  - sudo apt-get install python-httpretty

script:
  - sudo -H scripts/ci
